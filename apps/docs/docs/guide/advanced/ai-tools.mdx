---
title: 接入 Agent
description: 为你的项目配置 Agent Skills、MCP 服务器与文档索引，实现自动化补译
---

import { Prompt } from '@rspress/core/theme';

把 ai-i18n 接到 AI 编码工具时，可以通过三种途径增强 Agent 的能力：

1. **Skills**：安装任务指令集，告诉 Agent 如何按安全流程调用工具补译。
2. **MCP (Model Context Protocol)**：为各类 AI 编码工具提供安全读取和写入翻译文件的底层能力。
3. **文档索引 (llms.txt)**：提供机器友好的官方指南，让 Agent 自己查阅高级配置。

## Skills

你可以在宿主环境（终端或项目库）安装 `@ai-i18n` 提供的标准指令。

在仓库根目录执行：

```sh
npx skills add bosens-China/ai-i18n --skill use-ai-i18n-mcp integrate-ai-i18n -y
```

安装后，Skill 会被保存到 `.agents/skills/` 中。在之后的对话中，你可以直接告诉 Agent：“使用 `use-ai-i18n-mcp` 补译缺失的语言” 或 “使用 `integrate-ai-i18n` 指导项目接入”。

`integrate-ai-i18n` 内置了一张"主题 → 官方文档页面"的索引表：常见配置直接写在 Skill 本地文件里，无需联网；遇到详尽选项表或边界情况时，Agent 会自动抓取下方「文档索引 (llms.txt)」小节所依赖的同一套 Markdown 镜像页面，你不需要手动把链接丢给它。

## MCP

Agent 补译依赖 `@ai-i18n/mcp` 暴露的底层写入能力。你需要在你最喜欢的 AI IDE 或 CLI 工具中注册这个 MCP 服务器。

不同的工具注册方式略有不同：

### Codex

在 `~/.codex/config.toml` 中加入，或者写到项目的 `.codex/config.toml` 中：

```toml
[mcp_servers.ai_i18n]
command = "npx"
args = ["-y", "@ai-i18n/mcp@alpha"]
```

或者在 Codex 桌面端 **Settings → MCP servers → Add server → STDIO** 中填写相同的 `command` 与 `args`。

### Cursor

创建项目级 `.cursor/mcp.json`，或者写到全局的 `~/.cursor/mcp.json`：

```json
{
  "mcpServers": {
    "ai-i18n": {
      "command": "npx",
      "args": ["-y", "@ai-i18n/mcp@alpha"]
    }
  }
}
```

### Claude Code

推荐注册为 local server，避免把绝对路径提交：

```sh
claude mcp add --transport stdio --scope local ai-i18n -- npx -y @ai-i18n/mcp@alpha
```

### Antigravity

创建项目级 `.agents/mcp_config.json`：

```json
{
  "mcpServers": {
    "ai-i18n": {
      "command": "npx",
      "args": ["-y", "@ai-i18n/mcp@alpha"]
    }
  }
}
```

### MCP 能力与边界

接入后，Agent 会获得 `ai_i18n_discover`、列表与写入工具。注册命令不需要项目路径；
`ai_i18n_discover` 优先读取客户端暴露的 workspace roots，并返回可直接传给其他工具的绝对
`i18n_directory`。discover 从 `@ai-i18n/mcp@1.0.0-alpha.4` 起提供；如果工具列表只有三个
翻译工具，请升级并重启 MCP 进程。暂时无法升级时，可从目标应用的 Vite root 与
`aiI18n({ directory })` 推导最终绝对目录。不支持 roots 的客户端可让 Agent 传当前工作目录。

所有列表工具同时返回完整 JSON 文本与 MCP `structuredContent`。即使客户端不展示结构化
字段，Agent 也能读取明细；结果较大时继续按 `next_cursor` 分页。

**安全运作边界：**

1. **分开 AI 与人工结果**：`mode: "fill"` 只写 `translations.json`；
   `mode: "review"` 只写 `overrides.json`。MCP 不修改 extracted 或 locales。
2. **锁内重读与字段更新**：每批写入都取得跨进程锁并重读最新文件。普通补译只填充
   `null`；人工确认修订时可使用 `mode: "review"`。review 默认影响同一原文的全部调用，
   `review_scope: "message"` 只影响带显式 ID 的目标消息。
3. **自动校准与同步**：Vite Dev 运行时会自动重建 locales；否则在下一次
   `vite dev` 或 `vite build` 时完成。
4. **写入失败先重读**：message 不属于文件时，先运行 Vite Dev/Build，再重新 list 并原样复制
   `message_id`；不要用 `source` 字段代替。

## 文档索引 (llms.txt)

当你希望 Agent 能深入理解本插件的高级用法、配置项甚至特定错误处理，你可以直接在任务对话中丢入本站点的 `llms.txt` 链接：

`https://bosens-china.github.io/ai-i18n/llms.txt`

本官方文档站点基于 Rspress 构建并开启了 SSG-MD 特性，在根路径暴露了对大模型高度友好的纯 Markdown 索引及其全量文本库 (`llms-full.txt`)。
Agent 收到这个地址后，能自动抓取结构化、干净的教程原文并学习。

## 最佳补译流程示例

完成上述接入后，你可以把下面的提示词发给 Agent。Agent 会通过 Skill 中的指引调用 MCP 完成任务：

<Prompt
  eyebrow="给 Agent"
  title="批量补齐缺失翻译"
  description="复制后发送给 Agent，按 MCP 安全流程补译。"
  defaultCollapsed={false}
  prompt={`使用 use-ai-i18n-mcp：
1. 先调用 ai_i18n_discover；仅在返回多个候选时再读取 Vite 配置确认目标目录；
2. 使用返回的 i18n_directory 列出所有仍有缺失翻译的文件；
3. 按原文语义和项目现有术语补齐目标语言；
4. 普通补译只写入 null 值；只有我明确要求审校修订时才使用 mode: review，并确认是影响
   同一原文的全部调用，还是用 review_scope: message 只修改一个显式 ID；
5. 再次查询并报告剩余缺失项。`}
/>
