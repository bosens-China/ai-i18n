# @boses/docs

用户文档站（Astro Starlight）。内部 PRD 与 TODO 仍在仓库根目录 `docs/`。

```sh
# 在仓库根目录
pnpm --filter @boses/docs dev
pnpm --filter @boses/docs build
```

GitHub Pages 构建时设置 `DEPLOY_TARGET=pages`，以使用 `/ai-i18n` base。
