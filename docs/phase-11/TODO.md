# Phase 11 TODO

- [x] 实现共享 Vue adapter 和只读 `tRef()`。
- [x] 仅在 Vue 虚拟模块、自动导入与生成类型中暴露 `tRef`。
- [x] Analyzer 提取显式导入、别名与自动导入的 `tRef()`。
- [x] ESLint 接受 setup 初始化，并拒绝 template / render 中创建 `tRef()`。
- [x] 覆盖 Runtime、Vite、Vitest、Analyzer、ESLint 与 Vue build 集成测试。
- [x] 更新用户文档、包 README 与两个 ai-i18n skills。
- [x] 运行完整 Vitest、TypeScript、ESLint、构建与文档站验收。
